1) Making a description page for a store
2) Making a page with a list of every store, with links to 
the description page

--------------------------------------------------------------------------------
1) Add a "Worker" model, choose the attributes
2) Add a page to add a worker
3) Add a page to see every worker profile
